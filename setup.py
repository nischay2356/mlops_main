from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='its an big a project people',
    author='nisch',
    license='MIT',
)
